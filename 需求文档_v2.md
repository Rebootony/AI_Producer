# AI 制片（AI Producer）需求文档 v2

> 版本：v2（2026-06-19 迭代）
> 输入来源：① 现有代码库现状 ② 2026-06-07 与成永强的迭代会议《智能纪要》 ③ resource/skills.md ④ `资料/` 下成永强提供的真实项目样本（泰康之家、石头科技、国窖）与价格计算方式、环节解释
> 目的：作为本轮（约 2 天）网站更新的需求基线。本文档**只描述需求，不改动代码**，供张子雄 review 后再进入实现。
> 角色约定：本文中「老板/商务」在现阶段视为同一人。

---

## 0. 一句话结论

本轮**两条线并重**：①把主线升级为「**上传 Brief → 一键生成报价+排期 → 与 AI 实时对话调整**（二代）」——融资演示最能打的 Hero Flow；②同时把现有「**老板↔员工传话 Demo**（一代）」打磨到演示级（重点修转述准确性）。预算+报价、排期、传话转述均为 **P0**。

---

## 1. 背景与目标

- **产品定位**：面向广告影片制作公司的「AI 虚拟制片 / 项目经理」。不是点按钮的工具，而是**有职业判断、能控预算、能排期、能协调执行**的 AI。（参考 resource/extracted_info.md、skills.md 第 0 节）
- **本轮目标**：做出**融资可演示的 MVP**——核心功能（报价+排期的生成与实时修改）真实可用即可，不追求完整。
- **战略排序**（会议原话）：「争取先做出可实际使用的二代产品，再往回倒做一代产品的方案」。**张子雄本轮拍板：两条线并重**——二代生成器为新主线，一代传话 Demo 同步打磨到演示级（两天偏紧，见 §8 砍线）。
- **质量要求**：上一版用非 Claude 模型生成、问题较多。本轮要求**数字可靠**（演示时报价/小计/总价必须永远对得上）、交互稳定。

---

## 2. 现状盘点（Current State）

### 2.1 技术架构
| 层 | 技术 | 关键文件 |
|---|---|---|
| 前端 | React18 + Vite + TS + Tailwind + Zustand（装了 react-router 但未用） | `frontend/src/App.tsx`（store 切换视图）、`store/useStore.ts`、`components/ChatSidebar.tsx`、`Workspace.tsx` + `tabs/*` |
| 后端 | FastAPI + SQLAlchemy + SQLite（`sql_app_demo.db`） | `backend/main.py`、`ai_agent.py`、`models.py`、`prompt_manager.py` |
| 模型 | OpenAI 兼容接口，默认硅基流动 `deepseek`（`.env` 配置），支持 Function Calling（17 个工具） | `ai_agent.py` |
| 构建/分发 | `vite.config.ts` 用 `viteSingleFile` 打包成单个 HTML（即根目录 `Demo.html` 的来源）；dev 时 `/api` 代理到 `:8000` | — |

### 2.2 现有功能
- **登录**：两个角色——老板（boss）/ 员工（张导 employee）。无「商务」角色。
- **大盘总览（Dashboard）**：活跃项目数、管理总预算、AI 节省工时（写死 128h）、风险预警（写死 1）。项目卡片读 store。
- **项目看板（Workspace）5 个 Tab**：项目总览 / 执行排期 / 预算控制 / 资产管理 / 项目团队。
- **AI 聊天（ChatSidebar）**：老板↔AI↔员工 的传话闭环；3 秒轮询刷新；AI 调用 17 个工具读/改项目状态、向员工派活、向老板汇报。
- **后端数据模型**：User / Project / BudgetBreakdown / Crew / Asset / ChatThread / Message。
- **长期偏好**：`prompts/user_preferences.md` 文件存「以后都叫我老板」等指令。

### 2.3 ⚠️ 现状最大问题（决定本轮工作量）
1. **看板是「假数据」**：5 个 Tab 的内容基本是 React 组件里**写死的数组**（如 `BudgetTab` 的报价表、`TimelineTab` 的 5 周排期、`AssetsTab` 的文件列表），**不读后端**。只有「项目总预算 + 状态」在聊天后会同步。→ 「实时生成与实时修改」目前根本不成立。
2. **数据模型不够**：`BudgetBreakdown` 只有 `category / item_name / amount`，**缺单价、人数、天数、单位** ——无法表达成永强价格单那种「单价 × 人数 × 天数」的报价结构。
3. **没有生成能力**：没有「上传 Brief → 生成报价/排期」的任何逻辑；没有文件上传；没有价格单引擎。
4. **没有利润率/成本口径**：现在只有一个 `budget` 总数，没有 成本/利润率/实收 的概念。
5. **传话偶发幻觉**：`ai_agent.py` 里有大量 if/补丁在硬修「AI 替员工编造进度」「误存偏好」等问题（会议待办「对话转述修正」正指此处）。

---

## 3. 迭代输入解读（新信息）

### 3.1 会议《智能纪要》核心要点
- **MVP 核心**：根据 **Brief + 商务沟通记录 + 成本价格单** 三类输入，**先快速给出粗略报价单 + 排期，再实时调整**。
- **AI 角色**：**严厉的流程执行者 / AI 项目经理（制片）**。负责**预算控制**，用流程替代「人情世故」。**2000 元以内 AI 可做主；超预算直接打回，让人去找老板**。沟通可委婉，流程必须严格把关。
- **报价依据**：影片时长、执行天数、难度 + 项目溢价。导演费 3000–10000 不等；**无明确区间时默认按 3000 算**。
- **五项内容实时生成/修改**：预算、排期、项目总览……（对应看板 5 个 Tab）。**先做预算+报价、排期，中后期功能后置**。
- **预算↔排期关系**：**先定预算**；预算里的**拍摄天数影响排期**；**后期费用按分钟计**；预算定了排期随之固定。
- **排期**：含前期策划/沟通 + 中期拍摄 + 后期；可随实际调整，但**关键节点（A copy 交付、拍摄日）不能变**；按客户 Final 时间**倒推**。
- **角色**：老板（决策、关注总费用与利润率）/ 商务（对外沟通，现阶段=老板）/ AI 项目经理（执行与管控，对预算有初判与制约）。
- **权限**：只有老板能看预算。
- **待办**：①对话转述修正（AI 准确转述员工原话，不直接复刻）；②商务交流模拟（成永强提供仿真聊天记录+价格单）。

### 3.2 skills.md（16 项能力，作为 AI 领域知识/训练标签）
Brief 理解、目标与范围定义、自动排期、预算报价与利润管理、资源统筹、任务分配、向上汇报、向下管理、沟通协调、风险预警、创意转化、质量验收、供应商管理、变更管理、数据追踪、项目复盘。→ 本轮**喂给 AI 当系统知识**，并优先落地其中的**预算报价 / 自动排期 / 风险预警 / 向上汇报**。

### 3.3 `资料/` 真实样本——报价与排期模型（已逆向，详见附录 A）
- **报价单 = 4 个阶段**：A 前期筹备 / B 拍摄执行 / C 后期制作 / D 其他杂费。
- **计费公式**：`金额 = 单价 × 人数(QTY) × 天数(数量，仅 B 段)`；后期按**分钟**计；各段小计 → 成本核算 → **实收 = 成本 + 税点(~1%) + 利润，向上取整到整数**（石头 69272→实收 85000；国窖 31922→35000）。
- **基准单价**（价格计算方式.xls）：导演 3000、制片 1000、摄影 2000、焦点 1500、摄助 500、灯光师 1500、美术 2000、录音 2000、演员 1000、剪辑 1500/分、餐食 70/人天……单价随影片性质/难度/个人溢价浮动。
- **排期 = 周×天(MON–SUN) 网格**：每格填当日任务；按客户交付日**倒推**；节假日（春节）置灰；需客户配合的格子标红/黄。阶段链：需求沟通→脚本大纲→PPM→拍摄前准备→拍摄 DAYn→整理素材→Acopy(80%)→反馈/修改→TC 调色→Bcopy(95%)→修改→交付。

---

## 4. 目标产品：Hero Flow（融资演示主线）

> 这是演示时一镜到底要跑通的流程，也是本轮 P0 的全部意义。

1. **新建项目 / 上传**：老板（商务）新建项目，上传 **Brief（PDF）** 与 **商务沟通记录（可粘贴文本）**；价格单已预置后台。
2. **一键生成**：AI 解析 Brief → 自动产出**初版【报价单】**（4 段明细：单价×人数×天数、各段小计、成本/利润率/实收）**+【排期表】**（按交付日倒推的周×天网格）+ **【项目总览】**（客户/目标/周期/总价）。
3. **看板实时展示**：总览 / 预算 / 排期三个 Tab 直接读这份生成结果。
4. **对话实时改**：老板对 AI 说「导演按 5000 算」「拍摄加一天」「利润率提到 30%」「交付提前到 X 日」→ AI **即时重算报价 & 排期**，看板**实时刷新**。
5. **流程把关**：员工申请加预算 → AI 判断（**≤2000 自行批准；超预算打回**：「这超了，得找老板批」）。
6. **（保留）传话闭环**：老板↔AI↔员工 派活/汇报；修正转述准确性。

---

## 5. 需求清单（Requirements）

> 标签：`[保留]` 现有沿用 / `[改造]` 现有需重写 / `[新增]` ；优先级：P0=本轮必须、P1=本轮尽量、P2=后置。

### 5.1 报价 / 预算（核心）
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-B1 | 后台预置**价格单引擎**：基于 `价格计算方式.xls` 的 4 段基准单价、单位、定价区间，作为生成默认值 | [新增] | P0 |
| R-B2 | **报价数据模型**支持「阶段 / 项目 / 单价 / 人数 / 天数 / 单位 / 金额」，金额=单价×人数×天数（后期按分钟） | [改造] | P0 |
| R-B3 | 从 Brief + 商务记录**自动生成初版报价单**（LLM 抽参数 + 引擎算钱，见决策 D1） | [新增] | P0 |
| R-B4 | 报价看板：分段明细表 + 各段小计 + **成本 / 税点 / 利润率 / 实收** 四档 + 预算水位条 + **利润率滑杆（拖动即重算实收，默认 25%）** | [改造] | P0 |
| R-B5 | **对话实时改报价**：改单价/人数/天数/某项增删、改利润率 → 即时重算并落库、刷新看板 | [新增] | P0 |
| R-B6 | **超支管控**：记录超支项；AI 对员工申请按「≤2000 批 / 超则打回找老板」处理 | [改造] | P0 |
| R-B7 | 默认值规则：无区间信息时导演按 3000、其他取基准单价；缺人数/天数给合理默认并在 UI 标注「待确认」 | [新增] | P1 |
| R-B8 | 预算**仅老板可见**（员工视图隐藏金额） | [新增] | P1 |

### 5.2 排期（核心）
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-T1 | **排期数据模型**：阶段 / 任务 / 起止日 / 是否关键节点 / 是否需客户配合 / 状态 | [改造] | P0 |
| R-T2 | 由**拍摄天数 + 交付日倒推**自动生成排期（周×天网格，套用环节解释里的标准工期） | [新增] | P0 |
| R-T3 | 排期看板：周×天网格视图（或里程碑时间线），关键节点高亮、需客户配合标色、节假日置灰 | [改造] | P0 |
| R-T4 | **对话实时改排期**：「拍摄加一天」「交付提前」→ 重新倒推；关键节点（A copy、拍摄日）变更需二次确认 | [新增] | P1 |
| R-T5 | 预算↔排期联动：改拍摄天数同时影响 B 段报价与排期 | [新增] | P1 |

### 5.3 项目总览 / 大盘
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-O1 | 项目总览由生成结果驱动（客户/行业/目标/周期/交付/总价/健康度），替换写死内容 | [改造] | P0 |
| R-O2 | 大盘总览读真实项目聚合数据（项目数、总预算、风险数），去掉写死的 128h 等 | [改造] | P1 |
| R-O3 | 大盘风险预警：基于预算水位/排期延期自动算「需关注」 | [新增] | P2 |

### 5.4 AI 对话 / 流程执行
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-A1 | AI 人设升级为「严厉的流程执行者/项目经理」，把 skills.md 作为领域知识注入 system prompt | [改造] | P0 |
| R-A2 | 新增工具：生成报价、生成排期、改报价项、改利润率、改排期、超支审批 | [新增] | P0 |
| R-A3 | **转述修正**：员工回复→AI 准确转述要点给老板（原话放双引号），不复刻、不编造进度（重写现有补丁逻辑） | [改造] | P0 |
| R-A4 | 生成前**反向提问**：Brief 关键信息缺失（时长/交付日/拍几天/影片性质）时 AI 先追问再生成（skills.md 第 1 节） | [新增] | P1 |
| R-A5 | 风险预警话术：预算超标/节点临近时 AI 主动提示 | [新增] | P2 |

### 5.5 文件 / 资产
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-F1 | **文件上传**：Brief PDF（抽文本喂 LLM）、商务记录（粘贴文本）；价格单后台预置不需上传 | [新增] | P0 |
| R-F2 | 资产 Tab 读后端真实资产列表（含上传的 Brief/生成的报价/排期） | [改造] | P1 |
| R-F3 | 报价单/排期可导出（PDF 或 Excel），对客户/老板呈现 | [新增] | P2 |

### 5.6 角色 / 权限
| ID | 需求 | 标签 | 优先级 |
|---|---|---|---|
| R-R1 | 现阶段保留 老板 / 员工 两角色；「商务」与老板合并（老板视角即可上传+看报价+看利润） | [保留] | P0 |
| R-R2 | 预算/利润仅老板可见（见 R-B8） | [新增] | P1 |
| R-R3 | 独立「商务」角色与前期三方讨论会（商务+老板+AI） | [新增] | P2 |

---

## 6. 数据模型变更建议（供 review）

```
Project（扩展）
  + duration_minutes  影片时长(分钟)
  + difficulty        难度(低/中/高)
  + cost_total        成本核算
  + tax_rate          税点(默认1%)
  + margin_rate       利润率(默认0.25)
  + client_price      实收
  + film_type         影片性质(宣传片/TVC/创意视频…)

QuoteItem（新增，替代/扩展 BudgetBreakdown）
  id, project_id, phase(A/B/C/D), item_name,
  unit_price, qty_people, qty_days, unit(人*天/项/分钟…),
  amount(=unit_price*qty_people*qty_days), is_overrun, note

ScheduleItem（新增）
  id, project_id, stage, task, start_date, end_date,
  is_milestone, needs_client, status, day_cell(可选,用于周×天网格)

Asset（扩展）
  + file_path/url, uploaded_by, size, category
```
> 说明：是否保留旧 `BudgetBreakdown`/`Crew` 还是迁移到 `QuoteItem`，建议**新建 `QuoteItem` 并废弃 `BudgetBreakdown`**（人员信息可由 B 段 item 承载）。

---

## 7. 对「现有需求」的增 / 删 / 改（结合 skills.md 与会议建议）

- **改**：看板 5 Tab 从写死 → 后端数据驱动（R-O1/B4/T3/F2）。
- **改**：预算从「单个总数」→「分段明细 + 成本/利润/实收」（R-B2/B4）。
- **改**：AI 人设从「微信口语小助手」→「严厉流程执行者 + 领域知识」（R-A1）；但对外沟通仍可委婉。
- **改**：传话转述逻辑从「一堆 if 硬补丁」→ 规范的「转述/汇报」工具（R-A3）。
- **增**：上传、生成、实时重算、利润率、超支审批、倒推排期（第 5 章 P0 项）。
- **删/后置**：大盘里写死的「AI 节省 128h」「+2 本周」等伪指标先去掉或改真实（R-O2）；多角色协作、外部渠道（飞书/邮件）、复杂审批流**本轮不做**（DEMO_PROGRESS 已列为限制）。
- **保留**：老板↔员工传话闭环、单项目沙盒、SQLite 本地、单文件打包分发。

---

## 8. 两天 MVP 范围建议（按已确认决策：两条线并重）

> ⚠️ 老实说：「两条线并重 + 两套样本」两天**偏紧**。下面按 **Must（演示必须）/ Then（有时间再做）** 分层，先保证 Hero Flow 与传话闭环都能演示级跑通，P2 全砍。

**Day 1（后端 + 引擎）**
- Must：价格单引擎 + `QuoteItem`/`ScheduleItem` 模型（R-B1/B2/T1）。
- Must：生成接口——Brief/参数 →（**引擎算钱**）报价 + 排期（R-B3/T2）。
- Must：报价/排期「改」接口 + 对应 AI 工具（含改利润率）（R-B5/A2）。
- Then：超支审批工具（R-B6）。

**Day 2（前端 + AI + 联调）**
- Must：预算/排期/总览 三 Tab 接真实数据 + 实时刷新 + **利润率滑杆**（R-B4/T3/O1）。
- Must：Brief 上传入口（R-F1）。
- Must（传话线）：AI 人设升级 + **转述修正**（R-A1/A3）——传话 Demo 的演示级关键。
- Must：达梦走通 Hero Flow；泰康作为第二项目准备。
- Then：超支管控联调（R-B6）、排期改动二次确认（R-T4）。

**本轮不做（P2 全后置）**：导出、独立商务角色、外部通知、风险自动算分、多角色协作。

**时间不够时的砍线（从下往上砍）**：① 泰康第二套 → 只留达梦；② 排期实时改（R-T4）→ 排期先只生成不改；③ 超支审批 → 话术模拟。**底线不砍：报价「生成+实时改」与 传话「转述修正」。**

---

## 9. 已确认决策（2026-06-19 review）

- **D1｜生成方式＝引擎算钱 + LLM 抽参数/改动**：确定性价格单引擎负责所有计算（数字永远对得上），LLM 只读 Brief 抽参数、处理对话改动。
- **D2｜利润率＝默认毛利率 25%，前端滑杆可调**：看板展示 成本/利润率/实收 三档；老板拖滑杆即重算实收，对话也可改。
- **D3｜两条线并重**：报价+排期生成 与 老板↔员工传话 Demo 都要打磨到演示级（工作量偏大，砍线见 §8）。
- **D4｜输入形态**：Brief＝PDF 上传抽文本；商务记录＝粘贴文本；价格单后台预置。（默认采纳）
- **D5｜演示样本＝两套都准备**：达梦（resource/ 已有完整 Brief+报价+排期）保底先跑通；泰康作为第二项目。
  ⚠️ **依赖**：泰康目前缺 Brief 与商务聊天记录，需成永强按会议待办补齐；否则泰康只能用已有的价格单/周期表反推、Brief 暂留占位。

---

## 附录 A：报价 / 排期模型速查（来自 `资料/`）

**报价四段与计费**
- A 前期筹备：创意/脚本撰写、执行方案（单位：项）
- B 拍摄执行：导演/制片/摄影/焦点/摄助/灯光/美术/道具/录音/演员/服化/设备器材/场地（单位：人*天 或 项*天）；`金额=单价×人数×天数`
- C 后期制作：剪辑/包装/调色/视效/配乐/配音/素材（**按分钟/项**）
- D 其他杂费：餐食(70/人天)/货拉拉/机动费用/设备车…
- 合计：各段小计 → 成本核算 → +税点(~1%) +利润 → **实收（向上取整）**

**三个真实样本（验证模型）**
| 项目 | 成本 | 实收(含税) | 拍摄天数 |
|---|---|---|---|
| 泰康之家·海琴府 | 93048 | 92127 | 4 天 |
| 石头科技 | 69272 | 85000 | 1 天 |
| 国窖 | 31922 | 35000 | 1 天 |

**排期（周×天网格，倒推自交付日）**
需求沟通 → 脚本大纲(3-5天) → 脚本细化(2-3天)/PPM(内容准备·堪景·1st PPM·Final PPM) → 拍摄前准备 → 拍摄 DAY1..n → 整理素材 → **Acopy 提交(完成度80%)** → 客户反馈/修改 → **TC 调色(2天)** → **Bcopy(95%)** → 修改 → **交付(Final)**。关键节点（A copy 交付、拍摄日）锁定不可随意变；节假日置灰；需客户配合的日期标红/黄。

---

## 附录 B：参考文件索引
- 现状代码：`backend/ai_agent.py`（17 工具）、`backend/models.py`、`frontend/src/components/tabs/*`、`frontend/src/store/useStore.ts`
- 现有文档：`AI_Producer_Product_Features.md`、`DEMO_PROGRESS.md`、`TODO_LOG.md`
- 迭代输入：`智能纪要…2026年6月7日.pdf`、`resource/skills.md`、`资料/价格计算方式.xls`、`资料/部分环节解释.docx`、`资料/{1泰康,2石头科技,3国窖}/`、`resource/达梦…Brief.pdf` 与报价/周期表
```
